import { type FC } from 'react';
import { VideoForm } from './_components/video-form';

const Page: FC = () => {
  return (
    <main className="w-full max-w-lg">
      <VideoForm />
    </main>
  );
};

export default Page;
