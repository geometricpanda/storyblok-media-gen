import { type FC } from 'react';
import { ImageForm } from './_components/image-form';

const Page: FC = () => {
  return (
    <main className="w-full max-w-lg">
      <ImageForm />
    </main>
  );
};

export default Page;
